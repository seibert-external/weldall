---
"@weldall/discovery-proxy": minor
---

Version the discovery proxy as its own release unit, independent of the Weldall server. `changeset publish` creates a `@weldall/discovery-proxy@x.y.z` Git tag and GitHub release, and the Docker image release builds from that tag.
