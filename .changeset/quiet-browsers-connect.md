---
"@weldall/browser": minor
---

Add the browser-only Weldall SPA connection client with WebCrypto DPoP, IndexedDB persistence, device polling, rotating refresh, live ID-JAG exchange, remote status, and revocation.
