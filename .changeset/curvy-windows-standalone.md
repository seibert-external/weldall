---
"@weldall/cli": minor
---

Add complete Ubuntu, macOS, and Windows CLI support plus self-contained standalone release assets. Initial standalone assets are unsigned and should be verified with the published SHA-256 checksums.
