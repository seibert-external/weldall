---
"@weldall/weldall": patch
---

Publish the Weldall server as a Docker image. Each `@weldall/weldall` release is now built, tested against a clean PostgreSQL, and published to Docker Hub as `x.y.z`, `x.y`, `latest` and the commit SHA.
