---
"@weldall/discovery-proxy": patch
---

Publish the discovery proxy as a Docker image. Each `@weldall/discovery-proxy` release is now built, tested against a controlled TLS upstream, and published to Docker Hub as `x.y.z`, `x.y`, `latest` and the commit SHA.
