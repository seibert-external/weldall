---
"@weldall/cli": minor
---

Add `weldall skills list --agentic`, a TOON rendering of the visible catalog for agents to read. The header declares the row count, so an agent that reads a truncated catalog can tell. The shape is deliberately unstable and is not a substitute for `--json`.
