---
"@weldall/cli": minor
---

Make help strictly local, cache validated discovery and signing keys, reuse verified access sessions, prepare resource authorization once per process, add bounded offset pagination with JSON Lines output, and add cached skill search and positional skill aliases.
