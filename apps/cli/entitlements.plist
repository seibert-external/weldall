<?xml version="1.0" encoding="UTF-8"?>
<!--
  Entitlements for the Developer-ID-signed macOS standalone CLI (see
  .github/workflows/release-cli-assets.yml). Required under the hardened runtime:

  - allow-jit / allow-unsigned-executable-memory / disable-executable-page-protection:
    Bun embeds JavaScriptCore, which JIT-compiles at runtime.
  - disable-library-validation: Bun extracts the bundled @napi-rs/keyring .node addon
    to a temp path and dlopen()s it; that file is not signed with our Team ID, so
    library validation would otherwise block the load and every keychain access fails.

  Apple notarization accepts these keys for a Developer ID CLI.
-->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-executable-page-protection</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
