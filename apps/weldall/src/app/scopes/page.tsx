import { PublicPage } from "../_components/public-page";

export const dynamic = "force-dynamic";

export default async function ScopesPage() {
  return <PublicPage>{/* Scopes public page (placeholder) */}</PublicPage>;
}
