import { PublicPage } from "../_components/public-page";

export const dynamic = "force-dynamic";

export default async function ResourcesPage() {
  return <PublicPage>{/* Resources public page (placeholder) */}</PublicPage>;
}
