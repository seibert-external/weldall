{
  "name": "@weldall/discovery-proxy",
  "version": "0.0.0",
  "license": "FSL-1.1-ALv2",
  "private": true,
  "scripts": {
    "test": "sh scripts/test-upstream.sh",
    "image:build": "docker build --tag weldall-discovery-proxy:local .",
    "image:test": "pnpm image:build && sh scripts/test-image.sh"
  }
}
